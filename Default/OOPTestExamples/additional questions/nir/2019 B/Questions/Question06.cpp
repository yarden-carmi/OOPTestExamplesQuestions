// שאלה 6
// נתונות הגדרות של שתי מחלקות:
// לאילו משתנים של המחלקה A הפונקציה f יכולה לגשת?

class A {...};
class B : private A { void f() {...} ...};

/*
(א) רק לאילו שמוגדרים public
(ב) רק לאילו שמוגדרים protected
(ג) תשובות (א) ו-(ב) נכונות
(ד) אף תשובה אינה נכונה
*/
